import { test } from '@playwright/test';

test.describe('Test Groups Practice', () => {
  test('First test case', async ({ page }) => {
    
  });

  test('Second test case', async ({ page }) => {
    
  });

  test('Third test case', async ({ page }) => {
    
  });
});